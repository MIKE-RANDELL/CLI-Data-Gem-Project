require 'spec_helper'

describe Volunteer do
  it 'has a version number' do
    expect(Volunteer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
